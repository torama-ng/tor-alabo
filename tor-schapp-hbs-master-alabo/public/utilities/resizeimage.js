const sharp = require('sharp');
const fs = require('fs');

module.exports = function resize(path, format, width, height) {
    try {
        //
    } catch(err) {
        console.error(err)
    }
    
};